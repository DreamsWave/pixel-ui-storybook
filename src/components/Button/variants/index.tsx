import BasicButton from './BasicButton';
import BulkButton from './BulkButton';
import SquaredButton from './SquaredButton';
import MinimalisticButton from './MinimalisticButton';
import GlassmorphismButton from './GlassmorphismButton';

export const ButtonVariants = {
  basic: BasicButton,
  bulk: BulkButton,
  squared: SquaredButton,
  minimalistic: MinimalisticButton,
  glassmorphism: GlassmorphismButton,
};
